name: 유니버스 스크리닝

on:
  schedule:
    # 매일 (월~금) 23:00 UTC — EOD 배치(22:00) 완료 후 실행, 스크리닝 후 즉시 positions sync
    - cron: "0 23 * * 1-5"
  workflow_dispatch: # 수동 실행 가능

jobs:
  universe:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: 의존성 설치
        run: pip install -r scripts/requirements.txt

      - name: 유니버스 스크리닝
        run: python scripts/fetch_universe.py

      - name: 모멘텀 sync (universe → positions)
        run: python scripts/fetch_momentum.py

      - name: JSON 커밋 & 푸시
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add app/public/data/universe.json app/public/data/positions.json
          git restore --staged app/public/data/push_subscriptions.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "data: 유니버스 스크리닝 + positions sync $(date -u '+%Y-%m-%d')"
          git pull --rebase --autostash origin main && git push
