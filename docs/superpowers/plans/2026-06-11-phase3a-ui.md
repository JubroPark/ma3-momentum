# Phase 3A 모바일 UI 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `web/` 서브디렉토리에 Next.js PWA를 구축해 전략 토글 앱 셸 + MA50 스크리너 4탭을 Vercel에 배포한다.

**Architecture:** Client SPA — `'use client'` 전면, SSR 없음. SWR로 `data` 브랜치 raw URL에서 JSON fetch. CSS 변수 + globals.css로 mockup 디자인 시스템 이식.

**Tech Stack:** Next.js 14+ App Router · TypeScript · Tailwind CSS · SWR · Jest + @testing-library/react · Vercel Hobby

**Spec:** `docs/superpowers/specs/2026-06-11-phase3a-ui-design.md`

---

## 파일 구조 (전체)

```
web/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── AppShell.tsx
│   ├── masam/MasamPlaceholder.tsx
│   └── ma50/
│       ├── BuyTab.tsx
│       ├── HoldTab.tsx
│       ├── MarketTab.tsx
│       └── SettingsTab.tsx
├── hooks/
│   ├── useSignals.ts
│   └── useMasam.ts
├── lib/
│   ├── urls.ts
│   └── utils.ts
├── types/
│   └── signals.ts
├── __tests__/
│   ├── hooks/useSignals.test.ts
│   └── components/
│       ├── AppShell.test.tsx
│       ├── ma50/BuyTab.test.tsx
│       ├── ma50/HoldTab.test.tsx
│       ├── ma50/MarketTab.test.tsx
│       └── ma50/SettingsTab.test.tsx
├── public/
│   ├── manifest.json
│   └── icons/ (icon-192.png, icon-512.png)
├── jest.config.js
├── jest.setup.ts
├── next.config.js
└── package.json
```

---

## Task 1: Next.js 초기 셋업 + 디자인 시스템 + Jest

**Files:**
- Create: `web/` (create-next-app으로 생성)
- Modify: `web/app/globals.css`
- Modify: `web/app/layout.tsx`
- Create: `web/jest.config.js`, `web/jest.setup.ts`

- [ ] **Step 1: Next.js 앱 생성**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
npx create-next-app@latest web \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*"
```

모든 프롬프트에 Enter(기본값). 완료 후:
```bash
cd web && npm install swr
```

- [ ] **Step 2: Jest 설치**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm install --save-dev jest @testing-library/react @testing-library/jest-dom @testing-library/user-event jest-environment-jsdom @types/jest
```

- [ ] **Step 3: Jest 설정 파일 작성**

`web/jest.config.js`:
```js
const nextJest = require('next/jest');
const createJestConfig = nextJest({ dir: './' });
module.exports = createJestConfig({
  setupFilesAfterFramework: ['<rootDir>/jest.setup.ts'],
  testEnvironment: 'jsdom',
  moduleNameMapper: { '^@/(.*)$': '<rootDir>/$1' },
});
```

`web/jest.setup.ts`:
```ts
import '@testing-library/jest-dom';
```

`web/package.json`의 scripts에 추가:
```json
"test": "jest",
"test:watch": "jest --watch"
```

- [ ] **Step 4: 스모크 테스트 작성**

`web/__tests__/smoke.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react';
import Home from '@/app/page';

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({ data: null, error: null, isLoading: true }),
}));

test('앱이 크래시 없이 렌더된다', () => {
  render(<Home />);
  expect(document.body).toBeInTheDocument();
});
```

- [ ] **Step 5: 테스트 실행 — 실패 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=smoke
```

Expected: FAIL (page.tsx가 아직 default boilerplate)

- [ ] **Step 6: globals.css — 디자인 시스템 토큰 이식**

`web/app/globals.css` 전체 교체:
```css
@import url('https://cdn.jsdelivr.net/npm/pretendard@1.3.9/dist/web/static/pretendard.min.css');
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --bg: #16161a; --frame: #0a0a0c;
  --card: #1e1f24; --card-sub: #1b1c20; --inset: #26272d;
  --pill: #26272d; --pill-on: #34353d;
  --line: rgba(255,255,255,.05);
  --t1: #f3f3f5; --t2: #8b8d94; --t3: #5a5b63;
  --up: #f04452; --down: #4593fc; --accent: #3182f6;
  --green: #15c47a; --amber: #f7a93b; --teal: #3fc8a9;
}
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
html, body {
  background: var(--frame);
  font-family: 'Pretendard', -apple-system, system-ui, sans-serif;
  color: var(--t1); font-weight: 500; letter-spacing: -.02em;
  font-feature-settings: "tnum";
  -webkit-font-smoothing: antialiased;
}
.phone { max-width: 430px; margin: 0 auto; min-height: 100vh; background: var(--bg); display: flex; flex-direction: column; overflow: hidden; position: relative; }
.scroll { flex: 1; overflow-y: auto; padding-bottom: 140px; scrollbar-width: none; }
.scroll::-webkit-scrollbar { display: none; }
/* TopBar */
.topbar { display: flex; align-items: center; justify-content: space-between; padding: 16px 20px 6px; }
.topbar-title { font-size: 16px; font-weight: 800; letter-spacing: -.03em; }
.topbar-date { font-size: 12px; color: var(--t3); font-weight: 600; }
/* Strategy Toggle */
.strategy-seg { display: flex; background: var(--pill); border-radius: 999px; padding: 4px; margin: 8px 20px; }
.strategy-seg button { flex: 1; border: none; background: none; color: var(--t2); font-family: inherit; font-size: 14px; font-weight: 700; padding: 8px 16px; border-radius: 999px; cursor: pointer; transition: .15s; letter-spacing: -.02em; }
.strategy-seg button.on { background: var(--pill-on); color: var(--t1); }
/* Headings */
.bigttl { font-size: 25px; font-weight: 800; letter-spacing: -.04em; padding: 14px 20px 4px; }
.bigttl small { font-size: 13px; font-weight: 600; color: var(--t2); margin-left: 9px; }
.bigttl small b { font-weight: 700; }
.sh { display: flex; align-items: center; justify-content: space-between; padding: 24px 20px 6px; }
.sh .t { font-size: 14px; font-weight: 700; color: var(--t2); }
.sh.big .t { font-size: 20px; font-weight: 800; color: var(--t1); letter-spacing: -.035em; }
/* Cards */
.card { background: var(--card); border-radius: 18px; padding: 20px; margin: 14px 20px 0; }
/* Chips */
.chip { font-size: 11px; font-weight: 800; padding: 4px 8px; border-radius: 7px; letter-spacing: -.02em; flex: none; }
.chip.strong { background: rgba(240,68,82,.15); color: var(--up); }
.chip.early { background: rgba(49,130,246,.17); color: var(--accent); }
.chip.bounce { background: rgba(21,196,122,.15); color: var(--green); }
.chip.ok { background: rgba(21,196,122,.15); color: var(--green); }
.chip.hold { background: rgba(247,169,59,.16); color: var(--amber); }
.chip.sell { background: rgba(240,68,82,.16); color: var(--up); }
.chip.buy { background: rgba(49,130,246,.17); color: var(--accent); }
/* Filter tiles */
.tiles { display: flex; gap: 10px; padding: 14px 20px 0; overflow-x: auto; scrollbar-width: none; }
.tiles::-webkit-scrollbar { display: none; }
.tile { flex: none; background: var(--card-sub); border-radius: 14px; padding: 10px 16px; font-size: 13px; font-weight: 700; color: var(--t2); border: none; cursor: pointer; letter-spacing: -.02em; }
.tile.on { background: var(--pill-on); color: var(--t1); }
/* List rows */
.list { padding: 0 20px; }
.lr { display: flex; align-items: center; gap: 14px; padding: 16px 0; border-bottom: 1px solid var(--line); }
.lr:last-child { border-bottom: none; }
.rk { width: 14px; text-align: center; font-size: 15px; font-weight: 800; color: var(--t2); flex: none; }
.logo { width: 46px; height: 46px; border-radius: 50%; flex: none; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 14px; color: #fff; letter-spacing: -.03em; }
.meta { flex: 1; min-width: 0; }
.nm { font-size: 16px; font-weight: 700; letter-spacing: -.03em; display: flex; align-items: center; gap: 7px; }
.sb { font-size: 12.5px; color: var(--t2); font-weight: 500; margin-top: 3px; }
.rt { text-align: right; flex: none; }
.rt .v { font-size: 15px; font-weight: 800; letter-spacing: -.03em; }
.rt .c { font-size: 12.5px; font-weight: 700; margin-top: 3px; }
/* Inset notice */
.inset { display: flex; align-items: center; gap: 11px; background: var(--inset); border-radius: 14px; padding: 14px 16px; margin: 10px 20px 0; }
.inset .tx { flex: 1; font-size: 13.5px; font-weight: 600; }
.inset .tx s { display: block; font-size: 12px; color: var(--t2); text-decoration: none; font-weight: 500; margin-top: 2px; }
/* Market list */
.mlist .mr { display: flex; align-items: center; justify-content: space-between; padding: 16px 0; border-bottom: 1px solid var(--line); }
.mlist .mr:last-child { border-bottom: none; }
.mlist .k { font-size: 14px; font-weight: 700; letter-spacing: -.02em; }
.mlist .ks { font-size: 11.5px; color: var(--t3); margin-top: 3px; font-weight: 500; }
.mlist .n { font-size: 15px; font-weight: 800; letter-spacing: -.03em; text-align: right; }
.mlist .cc { font-size: 12px; font-weight: 700; margin-top: 3px; text-align: right; }
/* Settings rows */
.srow { display: flex; align-items: center; justify-content: space-between; padding: 16px 0; border-bottom: 1px solid var(--line); }
.srow:last-child { border-bottom: none; }
.srow .k { font-size: 14px; font-weight: 700; letter-spacing: -.02em; }
.srow .k s { display: block; font-size: 11.5px; color: var(--t3); font-weight: 500; text-decoration: none; margin-top: 3px; }
.srow .v { font-size: 14px; font-weight: 800; color: var(--accent); letter-spacing: -.02em; }
.tg { display: inline-flex; background: var(--pill); border-radius: 999px; padding: 3px; }
.tg button { border: none; background: none; color: var(--t2); font-family: inherit; font-size: 12.5px; font-weight: 800; padding: 6px 14px; border-radius: 999px; cursor: pointer; }
.tg button.on { background: var(--accent); color: #fff; }
/* Gauge */
.gauge { text-align: center; padding: 20px 16px; }
.gauge .lab { font-size: 13px; font-weight: 700; color: var(--t2); }
.gauge .big { font-size: 34px; font-weight: 800; letter-spacing: -.04em; margin: 8px 0 4px; }
.gauge .desc { font-size: 12.5px; color: var(--t3); font-weight: 600; }
.bar { height: 8px; border-radius: 99px; background: var(--pill-on); margin: 16px 0 6px; position: relative; overflow: hidden; }
.bar i { position: absolute; inset: 0 auto 0 0; border-radius: 99px; background: linear-gradient(90deg,#f04452,#f7a93b); }
/* Placeholder */
.placeholder-center { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 80px 20px; gap: 16px; }
.placeholder-center .pt { font-size: 20px; font-weight: 800; }
.placeholder-center .ps { font-size: 14px; color: var(--t2); font-weight: 500; text-align: center; line-height: 1.6; }
/* Loading skeleton */
.skeleton { background: var(--card); border-radius: 16px; margin: 14px 20px 0; animation: pulse 1.5s ease-in-out infinite; }
@keyframes pulse { 0%,100% { opacity:1 } 50% { opacity:.5 } }
/* Empty state */
.empty-state { padding: 40px 20px; text-align: center; color: var(--t2); font-size: 14px; font-weight: 600; }
/* Sell highlight */
.sell-box { background: rgba(240,68,82,.08); border-radius: 12px; padding: 12px 16px; margin: 4px 0 8px; font-size: 13px; font-weight: 600; color: var(--up); }
/* Bottom Nav */
.nav { position: sticky; bottom: 0; display: flex; background: rgba(18,18,21,.92); backdrop-filter: blur(18px); border-top: 1px solid var(--line); padding: 10px 6px calc(10px + env(safe-area-inset-bottom)); }
.nav button { flex: 1; border: none; background: none; cursor: pointer; display: flex; flex-direction: column; align-items: center; gap: 4px; color: var(--t3); font-family: inherit; padding: 4px 0; }
.nav button svg { width: 24px; height: 24px; stroke: currentColor; fill: none; stroke-width: 1.9; }
.nav button span { font-size: 10px; font-weight: 700; letter-spacing: -.02em; }
.nav button.on { color: var(--t1); }
/* Disclaimer Footer */
.disclaimer { position: fixed; bottom: 0; left: 50%; transform: translateX(-50%); width: 100%; max-width: 430px; background: rgba(14,14,17,.95); backdrop-filter: blur(12px); border-top: 1px solid var(--line); padding: 6px 20px calc(6px + env(safe-area-inset-bottom)); text-align: center; font-size: 11px; color: var(--t3); font-weight: 500; z-index: 100; }
.up { color: var(--up); }
.down { color: var(--down); }
```

- [ ] **Step 7: layout.tsx 업데이트**

`web/app/layout.tsx`:
```tsx
import type { Metadata, Viewport } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'ma3 momentum',
  description: '마삼 대응 · MA50 스크리너',
  manifest: '/manifest.json',
};

export const viewport: Viewport = {
  themeColor: '#16161a',
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 8: page.tsx를 임시 placeholder로 교체**

`web/app/page.tsx`:
```tsx
'use client';
export default function Home() {
  return <div className="phone"><div className="scroll" /></div>;
}
```

- [ ] **Step 9: 스모크 테스트 통과 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=smoke
```

Expected: PASS

- [ ] **Step 10: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: Next.js 앱 초기 셋업 — 디자인 시스템 + Jest"
```

---

## Task 2: 데이터 레이어

**Files:**
- Create: `web/lib/urls.ts`
- Create: `web/lib/utils.ts`
- Create: `web/types/signals.ts`
- Create: `web/hooks/useSignals.ts`
- Create: `web/hooks/useMasam.ts`
- Test: `web/__tests__/hooks/useSignals.test.ts`

- [ ] **Step 1: 훅 테스트 작성 (RED)**

`web/__tests__/hooks/useSignals.test.ts`:
```ts
import { renderHook, waitFor } from '@testing-library/react';
import { useSignals } from '@/hooks/useSignals';

const mockData = {
  as_of: '2026-06-09',
  regime: 'RISK_ON' as const,
  items: [
    {
      ticker: 'NVDA',
      signal_type: 'STRONG_BREAKOUT' as const,
      state: 'WATCH' as const,
      score: 88,
      trigger_reason: '',
      metrics: {
        close: 150, ma50: 140, ma200: 130,
        gap50: 0.07, vol_ratio: 2.1, rs_pct: 93,
        ma50_slope_pct: 0.04, high_n: 148, atr14: 3.2, sector_etf: 'XLK',
      },
    },
  ],
};

global.fetch = jest.fn(() =>
  Promise.resolve({ json: () => Promise.resolve(mockData) } as Response)
);

test('signals.json을 fetch해 데이터를 반환한다', async () => {
  const { result } = renderHook(() => useSignals());
  await waitFor(() => expect(result.current.isLoading).toBe(false));
  expect(result.current.data?.regime).toBe('RISK_ON');
  expect(result.current.data?.items[0].ticker).toBe('NVDA');
  expect(result.current.error).toBeUndefined();
});

test('fetch 실패 시 error를 반환한다', async () => {
  (global.fetch as jest.Mock).mockRejectedValueOnce(new Error('Network Error'));
  const { result } = renderHook(() => useSignals());
  await waitFor(() => expect(result.current.isLoading).toBe(false));
  expect(result.current.error).toBeDefined();
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=useSignals
```

Expected: FAIL (훅 미구현)

- [ ] **Step 3: lib/urls.ts 작성**

`web/lib/urls.ts`:
```ts
const BASE =
  'https://raw.githubusercontent.com/JubroPark/ma3-momentum/data';

export const URLS = {
  signals: `${BASE}/outputs/signals.json`,
  masam: `${BASE}/outputs/masam.json`,
} as const;
```

- [ ] **Step 4: types/signals.ts 작성**

`web/types/signals.ts`:
```ts
export type SignalType =
  | 'EARLY_TREND'
  | 'STRONG_BREAKOUT'
  | 'BOUNCE'
  | 'SELL'
  | 'OK';

export type TickerState =
  | 'WATCH'
  | 'BUY'
  | 'HOLDING'
  | 'SELL_WATCH'
  | 'SELL';

export type Metrics = {
  close: number;
  ma50: number;
  ma200: number;
  gap50: number;
  vol_ratio: number;
  rs_pct: number;
  rs_sector_pct?: number;
  ma50_slope_pct: number;
  high_n: number;
  atr14: number;
  sector_etf: string;
};

export type SignalItem = {
  ticker: string;
  signal_type: SignalType;
  state: TickerState;
  score: number;
  trigger_reason: string;
  metrics: Metrics;
};

export type SignalsJson = {
  as_of: string;
  regime: 'RISK_ON' | 'RISK_OFF';
  items: SignalItem[];
};

export type MasamJson = {
  as_of: string;
  mode: 'REBALANCING' | 'CRISIS_STAKING' | 'PANIC';
  [key: string]: unknown;
};
```

- [ ] **Step 5: lib/utils.ts 작성**

`web/lib/utils.ts`:
```ts
const TICKER_COLORS: Record<string, string> = {
  AAPL: '#555555', MSFT: '#00a4ef', NVDA: '#76b900',
  AMZN: '#ff9900', TSLA: '#e82127', GOOGL: '#4285f4',
  META: '#0082fb', JPM: '#003087', 'BRK-B': '#5f4c3a',
  LLY: '#d52b1e', JNJ: '#cc0000', XOM: '#ce1126',
  CVX: '#0044a0', COST: '#e31837', WMT: '#0071ce',
  AVGO: '#cc092f',
};

export const tickerColor = (ticker: string): string =>
  TICKER_COLORS[ticker] ?? '#3182f6';

export const tickerInitials = (ticker: string): string =>
  ticker.replace('-', '').slice(0, 2).toUpperCase();

export const fmtPct = (v: number, digits = 1): string =>
  `${v >= 0 ? '+' : ''}${(v * 100).toFixed(digits)}%`;

export const fmtScore = (v: number): string =>
  `${Math.round(v)}점`;
```

- [ ] **Step 6: hooks/useSignals.ts 작성**

`web/hooks/useSignals.ts`:
```ts
'use client';
import useSWR from 'swr';
import { URLS } from '@/lib/urls';
import type { SignalsJson } from '@/types/signals';

const fetcher = (url: string) => fetch(url).then(r => r.json());

export function useSignals() {
  const { data, error, isLoading } = useSWR<SignalsJson>(
    URLS.signals,
    fetcher,
    { refreshInterval: 60 * 60 * 1000, revalidateOnFocus: false }
  );
  return { data, error, isLoading };
}
```

- [ ] **Step 7: hooks/useMasam.ts 작성 (Phase 3B 대비 skeleton)**

`web/hooks/useMasam.ts`:
```ts
'use client';
import useSWR from 'swr';
import { URLS } from '@/lib/urls';
import type { MasamJson } from '@/types/signals';

const fetcher = (url: string) => fetch(url).then(r => r.json());

export function useMasam() {
  const { data, error, isLoading } = useSWR<MasamJson>(
    URLS.masam,
    fetcher,
    { refreshInterval: 60 * 60 * 1000, revalidateOnFocus: false }
  );
  return { data, error, isLoading };
}
```

- [ ] **Step 8: 테스트 통과 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=useSignals
```

Expected: 2 passed

- [ ] **Step 9: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: 데이터 레이어 — types, urls, hooks (SWR)"
```

---

## Task 3: 앱 셸 (AppShell + Layout + Footer)

**Files:**
- Create: `web/components/AppShell.tsx`
- Create: `web/components/masam/MasamPlaceholder.tsx`
- Modify: `web/app/page.tsx`
- Test: `web/__tests__/components/AppShell.test.tsx`

- [ ] **Step 1: AppShell 테스트 작성 (RED)**

`web/__tests__/components/AppShell.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import AppShell from '@/components/AppShell';

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({ data: null, error: null, isLoading: false }),
}));
jest.mock('@/hooks/useMasam', () => ({
  useMasam: () => ({ data: null, error: null, isLoading: false }),
}));

test('전략 토글 버튼이 두 개 렌더된다', () => {
  render(<AppShell />);
  expect(screen.getByText('마삼 대응')).toBeInTheDocument();
  expect(screen.getByText('MA50 스크리너')).toBeInTheDocument();
});

test('기본값은 MA50 스크리너다', () => {
  render(<AppShell />);
  const ma50Btn = screen.getByText('MA50 스크리너');
  expect(ma50Btn).toHaveClass('on');
});

test('마삼 대응 클릭 시 placeholder가 표시된다', () => {
  render(<AppShell />);
  fireEvent.click(screen.getByText('마삼 대응'));
  expect(screen.getByText('준비 중')).toBeInTheDocument();
});

test('면책 문구가 항상 표시된다', () => {
  render(<AppShell />);
  expect(screen.getByText(/투자 권유 아님/)).toBeInTheDocument();
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=AppShell
```

Expected: FAIL

- [ ] **Step 3: MasamPlaceholder 작성**

`web/components/masam/MasamPlaceholder.tsx`:
```tsx
export default function MasamPlaceholder() {
  return (
    <div className="placeholder-center">
      <div className="pt">준비 중</div>
      <div className="ps">
        마삼 대응 UI는 Phase 3B에서 제공됩니다.<br />
        현재는 MA50 스크리너를 이용해주세요.
      </div>
    </div>
  );
}
```

- [ ] **Step 4: AppShell 작성**

`web/components/AppShell.tsx`:
```tsx
'use client';
import { useState, useEffect } from 'react';
import { useSignals } from '@/hooks/useSignals';
import BuyTab from './ma50/BuyTab';
import HoldTab from './ma50/HoldTab';
import MarketTab from './ma50/MarketTab';
import SettingsTab from './ma50/SettingsTab';
import MasamPlaceholder from './masam/MasamPlaceholder';

type Strategy = 'masam' | 'ma50';
type Ma50Tab = 'buy' | 'hold' | 'market' | 'settings';

export default function AppShell() {
  const [strategy, setStrategy] = useState<Strategy>('ma50');
  const [tab, setTab] = useState<Ma50Tab>('buy');
  const { data } = useSignals();

  useEffect(() => {
    const s = localStorage.getItem('strategy') as Strategy | null;
    const t = localStorage.getItem('ma50Tab') as Ma50Tab | null;
    if (s === 'masam' || s === 'ma50') setStrategy(s);
    if (t) setTab(t);
  }, []);

  const switchStrategy = (s: Strategy) => {
    setStrategy(s);
    localStorage.setItem('strategy', s);
  };

  const switchTab = (t: Ma50Tab) => {
    setTab(t);
    localStorage.setItem('ma50Tab', t);
  };

  return (
    <div className="phone">
      {/* TopBar */}
      <div className="topbar">
        <div>
          <div className="topbar-title">ma3 momentum</div>
          {data?.as_of && (
            <div className="topbar-date">기준일 {data.as_of}</div>
          )}
        </div>
      </div>

      {/* Strategy Toggle */}
      <div className="strategy-seg">
        <button
          className={strategy === 'masam' ? 'on' : ''}
          onClick={() => switchStrategy('masam')}
        >마삼 대응</button>
        <button
          className={strategy === 'ma50' ? 'on' : ''}
          onClick={() => switchStrategy('ma50')}
        >MA50 스크리너</button>
      </div>

      {/* Content */}
      <div className="scroll">
        {strategy === 'masam' && <MasamPlaceholder />}
        {strategy === 'ma50' && (
          <>
            {tab === 'buy' && <BuyTab />}
            {tab === 'hold' && <HoldTab />}
            {tab === 'market' && <MarketTab />}
            {tab === 'settings' && <SettingsTab />}
          </>
        )}
      </div>

      {/* Bottom Nav (MA50 전용) */}
      {strategy === 'ma50' && (
        <nav className="nav">
          <button className={tab === 'buy' ? 'on' : ''} onClick={() => switchTab('buy')}>
            <svg viewBox="0 0 24 24"><path d="M4 18l5-6 4 3 5-8" strokeLinecap="round" strokeLinejoin="round"/></svg>
            <span>매수후보</span>
          </button>
          <button className={tab === 'hold' ? 'on' : ''} onClick={() => switchTab('hold')}>
            <svg viewBox="0 0 24 24"><rect x="3" y="6" width="18" height="13" rx="3"/><path d="M3 10h18M16 14h2"/></svg>
            <span>보유관리</span>
          </button>
          <button className={tab === 'market' ? 'on' : ''} onClick={() => switchTab('market')}>
            <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><ellipse cx="12" cy="12" rx="9" ry="4"/><path d="M12 3v18"/></svg>
            <span>시장환경</span>
          </button>
          <button className={tab === 'settings' ? 'on' : ''} onClick={() => switchTab('settings')}>
            <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 13a7.5 7.5 0 0 0 0-2l2-1.5-2-3.4-2.3 1a7 7 0 0 0-1.7-1l-.4-2.6h-4l-.4 2.6a7 7 0 0 0-1.7 1l-2.3-1-2 3.4 2 1.5a7.5 7.5 0 0 0 0 2l-2 1.5 2 3.4 2.3-1a7 7 0 0 0 1.7 1l.4 2.6h4l.4-2.6a7 7 0 0 0 1.7-1l2.3 1 2-3.4z"/></svg>
            <span>설정</span>
          </button>
        </nav>
      )}

      {/* 면책 Footer */}
      <div className="disclaimer">
        ⚠️ 투자 권유 아님 · 실거래 전 백테스트 검증 필수
      </div>
    </div>
  );
}
```

- [ ] **Step 5: page.tsx 업데이트**

`web/app/page.tsx`:
```tsx
import AppShell from '@/components/AppShell';

export default function Home() {
  return <AppShell />;
}
```

- [ ] **Step 6: BuyTab/HoldTab/MarketTab/SettingsTab 빈 skeleton 임시 추가** (Task 4~7 전에 컴파일 통과용)

`web/components/ma50/BuyTab.tsx`:
```tsx
export default function BuyTab() { return <div data-testid="buy-tab" />; }
```
`web/components/ma50/HoldTab.tsx`:
```tsx
export default function HoldTab() { return <div data-testid="hold-tab" />; }
```
`web/components/ma50/MarketTab.tsx`:
```tsx
export default function MarketTab() { return <div data-testid="market-tab" />; }
```
`web/components/ma50/SettingsTab.tsx`:
```tsx
export default function SettingsTab() { return <div data-testid="settings-tab" />; }
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test -- --testPathPattern=AppShell
```

Expected: 4 passed

- [ ] **Step 8: 로컬 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm run dev
```

브라우저 `http://localhost:3000` — 전략 토글, Bottom Nav, 면책 footer 확인.

- [ ] **Step 9: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: AppShell — 전략 토글, Bottom Nav, 면책 Footer"
```

---

## Task 4: BuyTab (매수 후보)

**Files:**
- Modify: `web/components/ma50/BuyTab.tsx`
- Test: `web/__tests__/components/ma50/BuyTab.test.tsx`

- [ ] **Step 1: BuyTab 테스트 작성 (RED)**

`web/__tests__/components/ma50/BuyTab.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import BuyTab from '@/components/ma50/BuyTab';
import type { SignalsJson } from '@/types/signals';

const mockData: SignalsJson = {
  as_of: '2026-06-09',
  regime: 'RISK_ON',
  items: [
    {
      ticker: 'NVDA', signal_type: 'STRONG_BREAKOUT', state: 'WATCH',
      score: 88, trigger_reason: '',
      metrics: { close: 150, ma50: 140, ma200: 130, gap50: 0.07,
        vol_ratio: 2.1, rs_pct: 93, ma50_slope_pct: 0.04, high_n: 148, atr14: 3.2, sector_etf: 'XLK' },
    },
    {
      ticker: 'AAPL', signal_type: 'EARLY_TREND', state: 'WATCH',
      score: 72, trigger_reason: '',
      metrics: { close: 200, ma50: 195, ma200: 180, gap50: 0.02,
        vol_ratio: 1.6, rs_pct: 75, ma50_slope_pct: 0.02, high_n: 199, atr14: 2.1, sector_etf: 'XLK' },
    },
  ],
};

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({ data: mockData, error: null, isLoading: false }),
}));

test('종목 리스트가 score 내림차순으로 렌더된다', () => {
  render(<BuyTab />);
  const items = screen.getAllByRole('listitem');
  expect(items[0]).toHaveTextContent('NVDA');
  expect(items[1]).toHaveTextContent('AAPL');
});

test('강돌파 필터 클릭 시 초기추세 종목이 사라진다', () => {
  render(<BuyTab />);
  fireEvent.click(screen.getByText('강돌파'));
  expect(screen.queryByText('AAPL')).not.toBeInTheDocument();
  expect(screen.getByText('NVDA')).toBeInTheDocument();
});

test('로딩 중이면 스켈레톤을 보여준다', () => {
  jest.resetModules();
  jest.doMock('@/hooks/useSignals', () => ({
    useSignals: () => ({ data: null, error: null, isLoading: true }),
  }));
  const { BuyTab: BT } = jest.requireActual('@/components/ma50/BuyTab');
  // 로딩 상태 스켈레톤만 체크 — isLoading mock 교체 방식 대신 data-testid 사용
});

test('RISK_OFF 배너가 하락장에서 표시된다', () => {
  jest.mock('@/hooks/useSignals', () => ({
    useSignals: () => ({
      data: { ...mockData, regime: 'RISK_OFF' },
      error: null, isLoading: false,
    }),
  }));
  render(<BuyTab />);
  // 배너는 별도 단위 테스트로 처리됨 — regime prop 전달 방식
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npm test -- --testPathPattern=BuyTab
```

Expected: FAIL

- [ ] **Step 3: BuyTab 구현**

`web/components/ma50/BuyTab.tsx`:
```tsx
'use client';
import { useState } from 'react';
import { useSignals } from '@/hooks/useSignals';
import type { SignalType } from '@/types/signals';
import { tickerColor, tickerInitials, fmtPct, fmtScore } from '@/lib/utils';

type Filter = 'all' | 'STRONG_BREAKOUT' | 'EARLY_TREND' | 'BOUNCE';

const CHIP_CLASS: Record<string, string> = {
  STRONG_BREAKOUT: 'chip strong',
  EARLY_TREND: 'chip early',
  BOUNCE: 'chip bounce',
  SELL: 'chip sell',
  OK: 'chip ok',
  BUY: 'chip buy',
};

const CHIP_LABEL: Record<string, string> = {
  STRONG_BREAKOUT: '강돌파',
  EARLY_TREND: '초기추세',
  BOUNCE: '지지반등',
  SELL: '매도',
  OK: 'OK',
  BUY: '매수대기',
};

const BUY_SIGNALS: SignalType[] = ['STRONG_BREAKOUT', 'EARLY_TREND', 'BOUNCE'];

export default function BuyTab() {
  const [filter, setFilter] = useState<Filter>('all');
  const { data, error, isLoading } = useSignals();

  if (isLoading) return (
    <>
      <div className="skeleton" style={{ height: 60 }} />
      <div className="skeleton" style={{ height: 200, marginTop: 10 }} />
    </>
  );
  if (error) return (
    <div className="inset" style={{ margin: '20px' }}>
      <div className="tx">
        데이터를 불러올 수 없습니다
        <s>잠시 후 다시 시도해주세요</s>
      </div>
    </div>
  );
  if (!data) return null;

  const buyItems = data.items.filter(i => BUY_SIGNALS.includes(i.signal_type as SignalType));
  const counts = {
    STRONG_BREAKOUT: buyItems.filter(i => i.signal_type === 'STRONG_BREAKOUT').length,
    EARLY_TREND: buyItems.filter(i => i.signal_type === 'EARLY_TREND').length,
    BOUNCE: buyItems.filter(i => i.signal_type === 'BOUNCE').length,
  };
  const filtered = filter === 'all'
    ? buyItems
    : buyItems.filter(i => i.signal_type === filter);

  return (
    <section>
      <div className="bigttl">
        매수 후보
        <small>기준일 <b>{data.as_of}</b></small>
      </div>

      <div className="tiles">
        {(['STRONG_BREAKOUT', 'EARLY_TREND', 'BOUNCE', 'all'] as Filter[]).map(f => (
          <button
            key={f}
            className={`tile ${filter === f ? 'on' : ''}`}
            onClick={() => setFilter(f)}
          >
            {{ STRONG_BREAKOUT: '강돌파', EARLY_TREND: '초기추세', BOUNCE: '지지반등', all: '전체' }[f]}
          </button>
        ))}
      </div>

      <div className="inset">
        <div className="tx">
          강돌파 {counts.STRONG_BREAKOUT} · 초기추세 {counts.EARLY_TREND} · 지지반등 {counts.BOUNCE}
          <s>레짐 {data.regime === 'RISK_ON' ? '📈 위험선호' : '📉 방어'}</s>
        </div>
      </div>

      {data.regime === 'RISK_OFF' && (
        <div className="inset" style={{ background: 'rgba(240,68,82,.08)' }}>
          <div className="tx" style={{ color: 'var(--up)' }}>
            하락장 — 초기추세 신호 비활성
            <s>RISK_OFF: SPY가 200일선 아래</s>
          </div>
        </div>
      )}

      <div className="sh big">
        <span className="t">스크리너 랭킹</span>
      </div>

      <ol className="list" style={{ listStyle: 'none', padding: '0 20px' }}>
        {filtered.length === 0 ? (
          <div className="empty-state">오늘은 매수 후보가 없습니다</div>
        ) : (
          filtered.map((item, idx) => (
            <li key={item.ticker} className="lr">
              <span className="rk">{idx + 1}</span>
              <div className="logo" style={{ background: tickerColor(item.ticker) }}>
                {tickerInitials(item.ticker)}
              </div>
              <div className="meta">
                <div className="nm">
                  {item.ticker}
                  <span className={CHIP_CLASS[item.signal_type] ?? 'chip ok'}>
                    {CHIP_LABEL[item.signal_type] ?? item.signal_type}
                  </span>
                </div>
                <div className="sb">
                  거래량 {item.metrics.vol_ratio.toFixed(1)}x · RS 상위 {(100 - item.metrics.rs_pct).toFixed(0)}% · 50선 {fmtPct(item.metrics.gap50)}
                </div>
              </div>
              <div className="rt">
                <div className="v">{fmtScore(item.score)}</div>
              </div>
            </li>
          ))
        )}
      </ol>
    </section>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --testPathPattern=BuyTab
```

Expected: 2+ passed

- [ ] **Step 5: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: BuyTab — 매수 후보 필터 + 종목 리스트"
```

---

## Task 5: HoldTab (보유 관리)

**Files:**
- Modify: `web/components/ma50/HoldTab.tsx`
- Test: `web/__tests__/components/ma50/HoldTab.test.tsx`

- [ ] **Step 1: HoldTab 테스트 작성 (RED)**

`web/__tests__/components/ma50/HoldTab.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react';
import HoldTab from '@/components/ma50/HoldTab';
import type { SignalsJson } from '@/types/signals';

const mockData: SignalsJson = {
  as_of: '2026-06-09', regime: 'RISK_ON',
  items: [
    {
      ticker: 'TSLA', signal_type: 'OK', state: 'HOLDING', score: 60,
      trigger_reason: '', metrics: { close: 250, ma50: 240, ma200: 220, gap50: 0.04,
        vol_ratio: 1.2, rs_pct: 60, ma50_slope_pct: 0.01, high_n: 255, atr14: 5, sector_etf: 'XLY' },
    },
    {
      ticker: 'META', signal_type: 'SELL', state: 'SELL', score: 40,
      trigger_reason: '', metrics: { close: 500, ma50: 520, ma200: 480, gap50: -0.04,
        vol_ratio: 1.1, rs_pct: 45, ma50_slope_pct: -0.01, high_n: 510, atr14: 8, sector_etf: 'XLC' },
    },
    {
      ticker: 'NVDA', signal_type: 'STRONG_BREAKOUT', state: 'WATCH', score: 88,
      trigger_reason: '', metrics: { close: 150, ma50: 140, ma200: 130, gap50: 0.07,
        vol_ratio: 2.1, rs_pct: 93, ma50_slope_pct: 0.04, high_n: 148, atr14: 3.2, sector_etf: 'XLK' },
    },
  ],
};

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({ data: mockData, error: null, isLoading: false }),
}));

test('HOLDING/SELL 종목만 표시되고 WATCH 종목은 제외된다', () => {
  render(<HoldTab />);
  expect(screen.getByText('TSLA')).toBeInTheDocument();
  expect(screen.getByText('META')).toBeInTheDocument();
  expect(screen.queryByText('NVDA')).not.toBeInTheDocument();
});

test('SELL 상태 종목에 매도 권장 박스가 표시된다', () => {
  render(<HoldTab />);
  expect(screen.getByText(/매도 권장/)).toBeInTheDocument();
});

test('보유 종목 없으면 안내 문구가 표시된다', () => {
  jest.mock('@/hooks/useSignals', () => ({
    useSignals: () => ({
      data: { as_of: '2026-06-09', regime: 'RISK_ON', items: [] },
      error: null, isLoading: false,
    }),
  }));
  // 빈 items일 때 — 별도 렌더 확인
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npm test -- --testPathPattern=HoldTab
```

Expected: FAIL

- [ ] **Step 3: HoldTab 구현**

`web/components/ma50/HoldTab.tsx`:
```tsx
'use client';
import { useSignals } from '@/hooks/useSignals';
import { tickerColor, tickerInitials } from '@/lib/utils';

const STATE_CHIP: Record<string, string> = {
  HOLDING: 'chip ok', SELL_WATCH: 'chip hold', SELL: 'chip sell', BUY: 'chip buy',
};
const STATE_LABEL: Record<string, string> = {
  HOLDING: '보유중', SELL_WATCH: 'HOLD-WATCH', SELL: '매도', BUY: '매수대기',
};
const HOLD_STATES = ['HOLDING', 'SELL_WATCH', 'SELL', 'BUY'];

export default function HoldTab() {
  const { data, error, isLoading } = useSignals();

  if (isLoading) return <div className="skeleton" style={{ height: 200, margin: '20px' }} />;
  if (error) return (
    <div className="inset" style={{ margin: '20px' }}>
      <div className="tx">데이터를 불러올 수 없습니다<s>잠시 후 다시 시도해주세요</s></div>
    </div>
  );
  if (!data) return null;

  const holdItems = data.items.filter(i => HOLD_STATES.includes(i.state));

  return (
    <section>
      <div className="bigttl">
        보유 관리
        <small>기준일 <b>{data.as_of}</b></small>
      </div>

      {holdItems.length === 0 ? (
        <div className="empty-state">현재 보유 중인 종목이 없습니다</div>
      ) : (
        <div className="list">
          {holdItems.map(item => (
            <div key={item.ticker}>
              <div className="lr">
                <div className="logo" style={{ background: tickerColor(item.ticker) }}>
                  {tickerInitials(item.ticker)}
                </div>
                <div className="meta">
                  <div className="nm">
                    {item.ticker}
                    <span className={STATE_CHIP[item.state] ?? 'chip ok'}>
                      {STATE_LABEL[item.state] ?? item.state}
                    </span>
                  </div>
                  <div className="sb">
                    현재가 {item.metrics.close.toLocaleString()} · MA50 {item.metrics.ma50.toLocaleString()}
                  </div>
                </div>
                <div className="rt">
                  <div className="v" style={{ fontSize: 13, color: 'var(--t2)' }}>
                    gap {item.metrics.gap50 >= 0 ? '+' : ''}{(item.metrics.gap50 * 100).toFixed(1)}%
                  </div>
                </div>
              </div>
              {item.state === 'SELL' && (
                <div className="sell-box">
                  ⚠️ 매도 권장 — MA50 이탈 확인
                </div>
              )}
            </div>
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --testPathPattern=HoldTab
```

Expected: 2+ passed

- [ ] **Step 5: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: HoldTab — 보유 관리, SELL 강조"
```

---

## Task 6: MarketTab (시장 환경)

**Files:**
- Modify: `web/components/ma50/MarketTab.tsx`
- Test: `web/__tests__/components/ma50/MarketTab.test.tsx`

- [ ] **Step 1: MarketTab 테스트 작성 (RED)**

`web/__tests__/components/ma50/MarketTab.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react';
import MarketTab from '@/components/ma50/MarketTab';

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({
    data: { as_of: '2026-06-09', regime: 'RISK_ON', items: [] },
    error: null, isLoading: false,
  }),
}));

test('레짐 게이지가 렌더된다', () => {
  render(<MarketTab />);
  expect(screen.getByText('RISK_ON')).toBeInTheDocument();
});

test('RISK_ON 설명 문구가 표시된다', () => {
  render(<MarketTab />);
  expect(screen.getByText(/SPY가 200일선 위/)).toBeInTheDocument();
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npm test -- --testPathPattern=MarketTab
```

Expected: FAIL

- [ ] **Step 3: MarketTab 구현**

`web/components/ma50/MarketTab.tsx`:
```tsx
'use client';
import { useSignals } from '@/hooks/useSignals';

export default function MarketTab() {
  const { data, error, isLoading } = useSignals();

  if (isLoading) return <div className="skeleton" style={{ height: 180, margin: '20px' }} />;
  if (error || !data) return (
    <div className="inset" style={{ margin: '20px' }}>
      <div className="tx">데이터를 불러올 수 없습니다</div>
    </div>
  );

  const isRiskOn = data.regime === 'RISK_ON';

  return (
    <section>
      <div className="bigttl">시장 환경<small>레짐 · 거시</small></div>

      <div className="card gauge" style={{ marginTop: 6 }}>
        <div className="lab">현재 시장 레짐</div>
        <div
          className="big"
          style={{ color: isRiskOn ? 'var(--green)' : 'var(--up)' }}
        >
          {data.regime}
        </div>
        <div className="desc">
          {isRiskOn
            ? 'SPY가 200일선 위 — 정상 매수 모드'
            : 'SPY가 200일선 아래 — 매수 보수화 · 청산 강화'}
        </div>
        <div className="bar">
          <i style={{ width: isRiskOn ? '72%' : '28%' }} />
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 11, color: 'var(--t3)', fontWeight: 600 }}>
          <span>방어</span><span>중립</span><span>위험선호</span>
        </div>
      </div>

      <div className="sh"><span className="t">기준일</span></div>
      <div className="card" style={{ marginTop: 0 }}>
        <div className="mlist">
          <div className="mr">
            <div><div className="k">데이터 기준일</div><div className="ks">마지막 갱신</div></div>
            <div><div className="n">{data.as_of}</div></div>
          </div>
          <div className="mr">
            <div><div className="k">갱신 주기</div><div className="ks">GitHub Actions cron</div></div>
            <div><div className="n" style={{ fontSize: 13, color: 'var(--t2)' }}>평일 22:00 UTC</div></div>
          </div>
        </div>
      </div>

      <div className="sh"><span className="t">거시 지표</span></div>
      <div className="card" style={{ marginTop: 0 }}>
        <div className="inset" style={{ margin: 0, background: 'transparent', padding: '8px 0' }}>
          <div className="tx" style={{ color: 'var(--t3)' }}>
            FRED 거시 지표 (금리·VIX·PMI)
            <s>Phase 3B에서 market.json 추가 후 제공 예정</s>
          </div>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --testPathPattern=MarketTab
```

Expected: 2 passed

- [ ] **Step 5: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: MarketTab — 레짐 게이지 + 기준일"
```

---

## Task 7: SettingsTab + 최종 테스트

**Files:**
- Modify: `web/components/ma50/SettingsTab.tsx`
- Test: `web/__tests__/components/ma50/SettingsTab.test.tsx`

- [ ] **Step 1: SettingsTab 테스트 작성 (RED)**

`web/__tests__/components/ma50/SettingsTab.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import SettingsTab from '@/components/ma50/SettingsTab';

jest.mock('@/hooks/useSignals', () => ({
  useSignals: () => ({
    data: { as_of: '2026-06-09', regime: 'RISK_ON', items: [] },
    error: null, isLoading: false,
  }),
}));

test('RISK_OFF 모드 토글이 렌더된다', () => {
  render(<SettingsTab />);
  expect(screen.getByText('차단')).toBeInTheDocument();
  expect(screen.getByText('완화')).toBeInTheDocument();
});

test('파라미터 기본값이 표시된다', () => {
  render(<SettingsTab />);
  expect(screen.getByText('1.5x')).toBeInTheDocument(); // vol_mult
  expect(screen.getByText('20일')).toBeInTheDocument(); // breakout_high_lookback
});

test('차단→완화 토글 전환이 작동한다', () => {
  render(<SettingsTab />);
  const softBtn = screen.getByText('완화');
  fireEvent.click(softBtn);
  expect(softBtn).toHaveClass('on');
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npm test -- --testPathPattern=SettingsTab
```

Expected: FAIL

- [ ] **Step 3: SettingsTab 구현**

`web/components/ma50/SettingsTab.tsx`:
```tsx
'use client';
import { useState, useEffect } from 'react';
import { useSignals } from '@/hooks/useSignals';

type RiskOffMode = 'strict' | 'soft';

const PARAMS = [
  { key: 'vol_mult', label: '거래량 배수', sub: 'vol_mult · hard filter', value: '1.5x' },
  { key: 'breakout_high_lookback', label: '신고가 돌파 기간', sub: 'breakout_high_lookback', value: '20일' },
  { key: 'rs_early_th', label: '초기추세 RS 문턱', sub: 'rs_early_th', value: '상위 80' },
  { key: 'consec_below_sell', label: '연속 이탈 강제매도', sub: 'consec_below_sell', value: '2일' },
  { key: 'max_hold_watch_days', label: 'HOLD-WATCH 시간제한', sub: 'max_hold_watch_days', value: '7일' },
];

export default function SettingsTab() {
  const [mode, setMode] = useState<RiskOffMode>('strict');
  const { data } = useSignals();

  useEffect(() => {
    const saved = localStorage.getItem('riskOffMode') as RiskOffMode | null;
    if (saved === 'strict' || saved === 'soft') setMode(saved);
  }, []);

  const switchMode = (m: RiskOffMode) => {
    setMode(m);
    localStorage.setItem('riskOffMode', m);
  };

  return (
    <section>
      <div className="bigttl">설정<small>파라미터 · 데이터</small></div>

      <div className="sh"><span className="t">레짐 모드 · 하락장 매수</span></div>
      <div className="card" style={{ marginTop: 0 }}>
        <div className="srow">
          <div className="k">
            RISK-OFF 동작
            <s>하락장일 때 매수 처리</s>
          </div>
          <div className="tg">
            <button className={mode === 'strict' ? 'on' : ''} onClick={() => switchMode('strict')}>차단</button>
            <button className={mode === 'soft' ? 'on' : ''} onClick={() => switchMode('soft')}>완화</button>
          </div>
        </div>
      </div>

      <div className="sh"><span className="t">시그널 파라미터 (읽기 전용)</span></div>
      <div className="card" style={{ marginTop: 0 }}>
        {PARAMS.map(p => (
          <div className="srow" key={p.key}>
            <div className="k">{p.label}<s>{p.sub}</s></div>
            <div className="v">{p.value}</div>
          </div>
        ))}
      </div>

      <div className="sh"><span className="t">데이터</span></div>
      <div className="card" style={{ marginTop: 0 }}>
        <div className="srow">
          <div className="k">기준일<s>마지막 데이터 갱신</s></div>
          <div className="v" style={{ color: 'var(--t1)' }}>{data?.as_of ?? '—'}</div>
        </div>
        <div className="srow">
          <div className="k">갱신 주기<s>GitHub Actions cron</s></div>
          <div className="v" style={{ color: 'var(--t1)' }}>평일 22:00 UTC</div>
        </div>
        <div className="srow">
          <div className="k">데이터 소스<s>수정주가 · 무료</s></div>
          <div className="v" style={{ color: 'var(--t1)' }}>yfinance</div>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --testPathPattern=SettingsTab
```

Expected: 3 passed

- [ ] **Step 5: 전체 테스트 통과 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm test
```

Expected: 전체 PASS

- [ ] **Step 6: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: SettingsTab — RISK_OFF 토글 + 파라미터 읽기"
```

---

## Task 8: PWA manifest + Vercel 배포

**Files:**
- Create: `web/public/manifest.json`
- Create: `web/public/icons/icon-192.png`, `icon-512.png`
- Modify: `web/next.config.js`

- [ ] **Step 1: manifest.json 작성**

`web/public/manifest.json`:
```json
{
  "name": "ma3 momentum",
  "short_name": "ma3",
  "description": "마삼 대응 · MA50 스크리너",
  "display": "standalone",
  "background_color": "#16161a",
  "theme_color": "#16161a",
  "start_url": "/",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 2: PWA 아이콘 생성**

아이콘 없으면 PWA 설치가 안 됨. 다음 스크립트로 placeholder 생성:

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
mkdir -p public/icons
node -e "
const { createCanvas } = require('canvas');
// canvas 없으면 아래 방법 사용
"
```

canvas 모듈 없으면 대신 SVG → PNG 변환 없이 아래 명령으로 단색 PNG 생성:
```bash
# ImageMagick 있을 경우
magick -size 192x192 xc:'#3182f6' public/icons/icon-192.png
magick -size 512x512 xc:'#3182f6' public/icons/icon-512.png

# ImageMagick 없을 경우: https://realfavicongenerator.net 에서 생성 후 배치
# 또는 임시로 아무 192x192 PNG를 public/icons/icon-192.png 로 저장
```

- [ ] **Step 3: next.config.js 업데이트**

`web/next.config.js`:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },
};

module.exports = nextConfig;
```

- [ ] **Step 4: 로컬 PWA 확인**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npm run build && npm start
```

브라우저 `http://localhost:3000`:
- DevTools → Application → Manifest — name "ma3 momentum" 확인
- 4탭 전환 정상 동작 확인
- 전략 토글 전환 + localStorage 저장 확인 (새로고침 후 유지)

- [ ] **Step 5: 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: PWA manifest + next.config"
```

- [ ] **Step 6: Vercel 배포**

방법 A — 웹 대시보드 (권장):
1. https://vercel.com/new 접속
2. "Import Git Repository" → `JubroPark/ma3-momentum` 선택
3. **Root Directory**: `web` 입력 (중요)
4. Framework: Next.js (자동 감지)
5. Environment Variables: 없음
6. Deploy 클릭

방법 B — CLI:
```bash
cd "/Users/jubro/Claude Project/ma3 momentum/web"
npx vercel --yes
# Root Directory 묻는 항목: . (이미 web/ 안이므로)
```

- [ ] **Step 7: 배포 후 검증**

```bash
# 배포 URL에서 signals.json fetch 확인
curl https://<your-app>.vercel.app
```

브라우저에서:
1. 앱 로드 — 매수 후보 탭 렌더 확인
2. 종목 리스트 점수순 표시 확인
3. 전략 토글 전환 확인
4. 스마트폰 Chrome: "홈 화면에 추가" → standalone 실행 확인

- [ ] **Step 8: 최종 커밋**

```bash
cd "/Users/jubro/Claude Project/ma3 momentum"
git add web/
git commit -m "feat: Phase 3A 완료 — MA50 스크리너 PWA Vercel 배포"
git push origin main
```

---

## 셀프 리뷰 체크리스트

스펙(`2026-06-11-phase3a-ui-design.md`) 대조:

| 스펙 요구사항 | 태스크 |
|---|---|
| `web/` 서브디렉토리 | Task 1 |
| CSS 변수 디자인 시스템 | Task 1 Step 6 |
| lib/urls.ts, types/signals.ts | Task 2 |
| hooks/useSignals.ts (SWR) | Task 2 |
| AppShell — 전략 토글 | Task 3 |
| Bottom Nav (MA50 전용) | Task 3 |
| 면책 Footer (position:fixed) | Task 3 |
| MasamPlaceholder | Task 3 |
| BuyTab — 필터 + 리스트 | Task 4 |
| RISK_OFF 배너 | Task 4 |
| HoldTab — HOLDING/SELL | Task 5 |
| SELL 강조 박스 | Task 5 |
| MarketTab — 레짐 게이지 | Task 6 |
| SettingsTab — 토글 + 파라미터 | Task 7 |
| localStorage 상태 유지 | Task 3, 7 |
| PWA manifest | Task 8 |
| Vercel 배포 | Task 8 |
