# 50일 이평선 매수/매도 스크리너 — 모바일 서비스 구현 스펙 v3

> US 전용 · 일별 EOD · Next.js PWA · 전면 무료 스택.
> ⚠️ 스크리닝 보조 도구이며 투자 권유 아님. 실거래 전 백테스트 검증 필수.

## 변경 이력 (v2 → v3, 2차 리뷰 반영)
- **RS: 참조 유니버스 + percentile rank** (워치리스트 z-score 왜곡 해소)
- **돌파 2트랙 분리**: A 초기추세(MA50) / B 강돌파(N신고가) — A에 안전장치 추가
- **거래량**: `vol_mult` hard filter 유지, VolMA3 상승은 옵션/스코어로 분리
- **HOLD-WATCH 판정 단순화**(MA200+RS) + 원칙(이격도·섹터·확신)은 score/컨텍스트로 보존
- **레짐 청산 강화**: RISK_OFF 시 HOLD 단축·`consec_below` 축소
- **포지션 레이어**(position_id/entry) 도입 — 스크리닝 state와 분리
- **포지션 사이징**(fixed-fractional / ATR) 백테스트 반영
- **score 명시 수식** 확정 + signals.json 필드 보강
- 이상치 필터 = 자동폐기 아닌 검수 플래그

---

## 0. 확정 결정사항
| 항목 | 값 |
|---|---|
| 대상 시장 | US 전용 (NYSE/NASDAQ) |
| 갱신 | 일별 EOD, 미국 장 마감 후 1회 배치 |
| 플랫폼 | Next.js 14+ PWA, Vercel Hobby |
| 워치/보유 | 사용자 등록제 |
| 참조 유니버스 | **S&P500 + NASDAQ100 (RS 산출용)** |
| 벤치마크/섹터 | SPY + SPDR 11개 섹터 ETF |
| 비용 | 전면 무료 |

---

## 1. 시그널 로직

표기: `close[0]`=오늘. 임계값은 전부 파라미터(§6).

### 1-1. 지표
- `MA50`, `MA200`, `VolMA20`, `VolMA3`
- `MA50_slope_pct` = `(MA50[0]−MA50[10])/MA50[10]` (또는 회귀 기울기)
- `gap50`=`(close−MA50)/MA50`, `gap200`=`(MA50−MA200)/MA200`
- `highN`=`max(high[1..breakout_high_lookback])`
- `ATR14` (사이징·이상치용)
- **RS (개선):**
  - `RS_raw` = `0.5·(r20ₛ−r20ₘ)+0.3·(r60ₛ−r60ₘ)+0.2·(r120ₛ−r120ₘ)` (vs SPY)
  - **`RS_pct`** = **참조 유니버스(S&P500+NDX) 내 `RS_raw`의 percentile rank (0~100)** ← 주 지표
  - `RS_z` = 참조 유니버스 z-score (옵션). 워치리스트만으론 산출 금지(표본 부족).
  - `RS_sector_pct` = 섹터 ETF 기준 동일.
  - ※ 참조 유니버스는 일별 종가만 롤링 캐시(전체 히스토리 매일 재수집 X) → 무료 한도 유지.

### 1-2. 레짐 필터
```
regime = (SPY_close > SPY_MA200) ? RISK_ON : RISK_OFF
```
| | RISK_OFF 동작 |
|---|---|
| 매수(strict, 기본) | BUY 전면 비활성 |
| 매수(soft) | `vol_mult↑`, `overshoot_max↓`, Track B만 허용 |
| **청산(신규)** | `max_hold_watch_days` 단축(예 3) · `consec_below_sell` 축소(예 1) |

### 1-3. 매수 — 2트랙 분리
공통 게이트: 레짐 통과, `gap50 ≤ overshoot_max`, `volume[0] ≥ vol_mult·VolMA20`(hard).

**Track B — 강돌파 (STRONG_BREAKOUT, 기본):**
```
선행: close[1] ≤ MA50[1] AND 최근 lookback_below일 close<MA50 ≥ 60%
돌파: close[0] > MA50[0]  AND  close[0] > highN     # N일 신고가 (fake breakout 차단)
```
**Track A — 초기추세 (EARLY_TREND, 옵션·공격형):**
```
돌파: close[0] > MA50[0] AND close[1] ≤ MA50[1]      # 신선한 50선 크로스 (highN 미충족 허용)
안전장치(필수): RS_pct ≥ rs_early_th (예 80)  AND  MA50_slope_pct > 0
```
> A는 B보다 이르지만 위험 — RS 상위·상승기울기·거래량으로 보완해 가짜돌파 회귀 방지. `signal_type`으로 구분.

**Track Bounce — 50선 지지 후 반등 (BOUNCE):** (v2 동일)
```
추세: 최근~20일 close>MA50 다수 AND slope_pct>0 AND MA50>MA200
근접: min(low[0..k]) ≤ MA50·(1+proximity_pct)
지지: min(close[0..k]) ≥ MA50·(1−break_tol)
반등: close[0]>close[1] AND close[0]>MA50
```
거래량 secondary(`VolMA3[0]>VolMA3[1]`)는 **옵션 플래그 + score 가산** (hard filter 아님).

### 1-4. 매도 + 3-State (판정 단순화)
`breakdown = close[0] < MA50·(1−sell_tol) OR (close[0]<MA50 AND close[1]<MA50)`

**상태 결정 (위→아래 우선):**
| # | 조건 | 결과 |
|---|---|---|
| 1 | `close ≥ MA50` | **OK** (리셋) |
| 2 | `close<MA50` 연속 `consec_below_sell`일 | **SELL(강제)** |
| 3 | HOLD-WATCH `days_in_state > max_hold_watch_days` | **SELL(강제)** |
| 4 | `breakdown` AND `close < MA200` | **SELL** |
| 5 | `breakdown` AND **`close>MA200` AND `RS_pct>50`** | **HOLD-WATCH** |
| 6 | `breakdown` (그 외) | **SELL** |

- HOLD 진입 판정은 #5로 **단순·설명가능**.
- **원칙 보존(삭제 X)**: 200선 이격수렴(`gap200`)·섹터 모멘텀·확신태그는 **score 가중 + UI 컨텍스트**로 표시되어 HOLD/SELL 우선순위(랭킹)에 반영. (예: 확신태그+이격수렴이면 같은 HOLD라도 상단 노출, 사용자 보류 판단 지원.)
- `consec_below_sell`·`max_hold_watch_days`는 RISK_OFF에서 자동 축소(§1-2).

### 1-5. 스크리닝 상태 머신 (ticker 단위)
`WATCH → BUY → HOLDING → SELL-WATCH → SELL → WATCH`
저장: `state, prev_state, entered_at, days_in_state, last_signal_date`. 전이 시 `days_in_state=0`. 전이 기록 append.

### 1-6. 포지션 레이어 (신규 · 매매 단위)
스크리닝 state와 **분리**. 재진입 이력 꼬임 방지.
```
position = { position_id, ticker, status: OPEN|CLOSED,
             entry_date, entry_price, size, stop_level,
             exit_date, exit_price, realized_pnl }
```
- 동일 종목 재진입 = **새 position_id**.
- HOLD-WATCH 경과일·손익은 **포지션 단위**로 추적.
- 손절선 `stop_level` = entry 기준 ATR 배수 또는 MA50 하단(둘 중 보수적).

---

## 2. 스코어 공식 (확정)
각 컴포넌트 0~1 정규화 → 가중합 ×100.
```
vol_score      = min(vol_ratio / vol_cap, 1)            # vol_ratio=volume/VolMA20, vol_cap=3
breakout_score = min(gap50 / overshoot_max, 1)          # 방어선 위 돌파폭
rs_score       = RS_pct / 100
trend_score    = 0.5·min(MA50_slope_pct / slope_cap, 1) + 0.5·(MA50>MA200 ? 1 : 0)

score = 100 · ( w1·vol_score + w2·breakout_score + w3·rs_score + w4·trend_score )
보너스(옵션): VolMA3 상승 +b, 이격수렴+확신태그 +c   # HOLD 랭킹용
```
기본 가중 `w1..w4 = 0.25` (튜닝 대상), `slope_cap=0.05`.

---

## 3. 데이터 레이어 (US · 무료)
| 구분 | 무료 소스 | 비고 |
|---|---|---|
| 일봉 | Stooq(CSV·무키) / yfinance | 수정주가 필수 |
| 참조 유니버스 | S&P500+NDX 일별 종가 롤링 캐시 | RS percentile |
| 벤치마크/섹터 | SPY + SPDR 11 ETF | |
| 거시 | FRED(`DFF`,10Y) | |
| PMI | 월 1회 수동 입력 | |
| 펀더멘털 | 사용자 체크리스트 | |

---

## 4. 연산 파이프라인 (일별)
```
[GitHub Actions Cron · 무료 · 18:00 ET]
1. 워치+보유+참조유니버스+SPY+섹터ETF EOD 수집 (거래일·서머타임 반영)
2. 지표 산출 (MA·Vol·slope·gap·ATR·highN·regime)
3. RS_raw → 참조 유니버스 percentile rank(RS_pct)
4. 직전 스냅샷 로드 → 2트랙 매수/3-state 매도 평가 → score 산출
5. ticker state + position 갱신(days_in_state·전이기록) → 저장
   (무료: repo JSON / Gist / Supabase·Upstash free)
6. 신규 BUY/SELL → Web Push(VAPID, 무료)
[PWA] 정적 signals.json fetch → 렌더
```

---

## 5. UX / IA (4-탭)
① 매수 후보(점수순, signal_type 배지 STRONG/EARLY/BOUNCE, RISK_OFF 배너) · ② 보유 관리(포지션 단위 상태칩+경과일+손익+손절선) · ③ 시장 환경(regime·연준·PMI·VIX) · ④ 종목 상세/설정(차트·펀더멘털 체크리스트·파라미터·전이기록). 차트: lightweight-charts.

---

## 6. 파라미터
| 파라미터 | 기본 | 설명 |
|---|---|---|
| `breakout_high_lookback` | 20 | Track B 신고가 기간 |
| `vol_mult` / `vol_cap` | 1.5 / 3 | 거래량 floor(hard) / score 상한 |
| `rs_early_th` | 80 | Track A RS percentile 문턱 |
| `lookback_below` | 7 | 돌파 전 하회 관찰 |
| `overshoot_max` | 7% | 추격 상한 |
| `proximity_pct`/`break_tol`/`sell_tol` | 3%/1%/1% | |
| `consec_below_sell` | 2~3 (OFF:1) | 연속 이탈 강제매도 |
| `max_hold_watch_days` | 5~10 (OFF:3) | HOLD 시간제한 |
| `conv_th` | 4% | 50/200 이격 수렴(score) |
| `rs_weights` | .5/.3/.2 | 20/60/120D |
| `score_w1..4` | .25 each | vol/breakout/rs/trend |
| `slope_cap` | 5% | trend_score 정규화 |
| `regime_mode` / `volconfirm` / `slope_method` | strict / base / pct | |
| `risk_per_trade` | 10% | fixed-fractional |
| `atr_mult` | 2.0 | ATR 손절/사이징 |

---

## 7. 백테스트 & 검증 (보강)
- **생존편향 제거**: point-in-time 유니버스(상폐·교체 포함).
- **체결**: 시그널=종가 산출 → **익일 시가 체결**(look-ahead 방지) + 슬리피지(bps) + 커미션.
- **포지션 사이징**: fixed-fractional(`risk_per_trade`) 기본, **ATR 기반**(`atr_mult`) 옵션 — 미적용 시 성과 왜곡.
- **데이터 정합성**: 수정주가 분할·배당 검증, **이상치(±20%/일)는 자동폐기 X → 검수 플래그**(실적 갭·정상 급등 오탐 방지) 후 코퍼레이트 액션 대조.
- 지표: 승률·평균손익·R-multiple·MDD·노출기간·휩쏘율.
- 튜닝: 그리드/베이지안 + **워크포워드**(인/아웃샘플 분리).

---

## 8. 스택 & 로드맵
**스택(무료)**: Next.js14+/TS/Tailwind/lightweight-charts/Vercel Hobby + GitHub Actions cron + 무료 저장 + Web Push.

| Phase | 내용 |
|---|---|
| 0 데이터 PoC | Stooq/yfinance EOD + 참조유니버스 RS percentile + MA/신고가 산출 검증 |
| 1 시그널 엔진 | 2트랙·3-state·score + 백테스트(익일시가·사이징) + 튜닝 |
| 2 백엔드 | Actions cron + ticker state/**position** persistence + 저장 + API |
| 3 모바일 UI | 4탭·포지션 관리·상세 차트 |
| 4 알림·설정 | Web Push·파라미터·펀더멘털 |
| 5 검증·최적화 | 워크포워드·실데이터 보정 |

---

## 9. signals.json 스키마 (확정)
```json
{
  "as_of": "2026-06-05",
  "regime": "RISK_ON",
  "items": [
    {
      "ticker": "AAPL",
      "signal_type": "STRONG_BREAKOUT",   // EARLY_TREND | STRONG_BREAKOUT | BOUNCE | SELL | HOLD_WATCH | OK
      "state": "BUY",
      "prev_state": "WATCH",
      "score": 82,
      "regime": "RISK_ON",
      "trigger_reason": "20D 신고가 돌파 + 거래량 1.8x + RS 상위 12%",
      "position_id": null,
      "entered_at": "2026-06-05",
      "days_in_state": 0,
      "metrics": {
        "close": 201.3, "ma50": 195.2, "ma200": 188.0,
        "gap50": 0.031, "gap200": 0.038, "vol_ratio": 1.8,
        "rs_pct": 88, "rs_sector_pct": 75, "ma50_slope_pct": 0.012,
        "high_n": 199.8, "atr14": 4.1, "sector_etf": "XLK", "sector_ok": true
      }
    }
  ]
}
```
- 별도 `positions.json`(포지션 단위) + `transitions.json`(append-only 전이기록).

---

## 10. 엣지 & 리스크
- MA200 산출 불가(신규상장) → 스킵. · 분할·배당 → 수정주가.
- 휩쏘 → 허용오차·연속일·신고가. · 데이터 지연 → 보류 플래그·상태 동결.
- 참조 유니버스 결측 종목 → RS percentile 산출서 제외.

## 11. 확장 (구조 유지)
백테스트 상세 · 파라미터 최적화(워크포워드/베이지안) · 포트폴리오 리스크(동시보유 한도·섹터 편중 제한·상관 기반 사이징).
